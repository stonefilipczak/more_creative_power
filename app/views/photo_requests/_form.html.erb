<%= form_with(model: photo_request, local: true) do |form| %>
<% if photo_request.errors.any? %>
<div id="error_explanation">
    <h2><%= pluralize(photo_request.errors.count, "error") %> prohibited this photo_request from being saved:</h2>

    <ul>
        <% photo_request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
    </ul>
</div>
<% end %>


<div class="field">
    <%= form.label :schedule %>
    <%= form.text_area :schedule %>
</div>

<div class="field">
    <%= form.label :contact %>
    <%= form.text_area :contact %>
</div>



<div class="actions">
    <%= form.submit %>
</div>
<% end %>